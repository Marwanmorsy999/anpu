# Normalize line endings across Windows/macOS/Linux checkouts.
# Go source is compiled the same either way, but shell installers break
# under CRLF, and gofmt expects LF (CI runs gofmt on Linux).
* text=auto
*.go text
*.sh text eol=lf
*.ps1 text eol=lf
*.yaml text eol=lf
*.yml text eol=lf
