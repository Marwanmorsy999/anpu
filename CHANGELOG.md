# Changelog

## Unreleased — engine quality series (Phases 0–6)

- **Phase 0 safety net**: hermetic unit tests + fixtures (soft-404 shells, WAF block, Burp/ZAP/HAR/Nuclei samples) for DedupKey, scoring, diff/drift, import parsers, RiskGrade/CSV/MD, soft404 Jaccard, and wrapper host/parser helpers — `go test ./...` is no longer vacuous.
- **Phases 1–2 finding credibility + FP hardening**: corroboration contract (High needs baseline + control + second signal; single-technique capped + needs-review); XSS baseline/control/context, cmdi baseline-subtract + echo-guard (never Critical), smuggling/timing caps, Nuclei/ZAP evidence-unavailable caps, dedup `disputed-sources` path; shared `fpmatch` package, fixed soft404 core, root-shell + WAF vetoes across exposure engines, expanded CDN matching, archiveurls/originip dead-logic fixes.
- **Phase 3 ultra confirmations**: XSS second tag family → High confidence, cmdi second metachar family clears review, blind-timing delay scaling → High/High (smuggling deliberately uncapped); soft404 baselines probe random paths even for query-bearing targets.
- **Phase 4 wrapper honesty**: `anpu tools` reports all 84 tools with real states/prereqs/costs/hints; public-suffix host handling; ANSI-tolerant parsers with loud JSON-drift warnings; fail-open yield/API gates; `ANPU_TOOL_BUDGET_SEC` cumulative cap.
- **Phase 5 operator UX**: `scan --csv/--md`, `query --dir`, `history --target/--json`, `show --severity/--limit/--long` + phase timings, shared target normalization, documented diff/drift/watch identity models, dead terminal banner removed, worst-first HTML.
- **Phase 6 decomposition**: `scan.go` 1818 → ~740 lines (`runtime.go`, `client.go`, `reports.go`, `pipeline_stages.go`, `modules.go`, `targets.go`); checkpoints versioned (legacy/future files restart fresh with a warning). Pre/post binaries verified byte-identical findings on advanced + ultra fixture scans.

## 0.3.1 (Unreleased)

- **Wrapper vectors go live (verified against owned targets)**: `openredirex` runs stdin URLs at `-c 10` with a `[FOUND]`-chain parser (Medium, redirectpack bar); `dotdotpwn` runs the http-module read-only confirm (`-f /etc/hosts`, depth 4, adversarial-gated) with a `VULNERABLE!` parser (High, lfipack bar) — `-k localhost` per upstream EXAMPLES, since the verdict is pattern-gated and `/etc/hosts` never contains `root:`. Both stages show `[ok]` with zero warnings on clean targets.
- **Jsluice native port (upstream archived, technique ported not wrapped)**: greedy quoted-string URL harvest with browser-style joinery (rooted, protocol-relative, asset-relative) plus a 14-pattern secret pack over already-fetched asset bodies (≤9 requests, dedup-aligned titles). Fixture-verified; bare relative refs must contain a path separator so minified-bundle fragments (`.split(`, `Math.trunc(`) never become endpoints — vondera.app diff holds at endpoints ±0.
- **Slowest-stage ledger**: top-5 slowest stages persisted into terminal (`Slowest:`), HTML, and JSON (`slowest_stages`) next to the phase ledger; wrapper verbose receipts now log yield-vs-time. Amass yield gate observed firing twice on rich native yield (121/118 hosts, 0s burned) — demotion deferred, the gate is working as designed.
- **CI hardening**: lint gate migrated to golangci-lint v2 (v1 binaries refuse go1.25+ targets; v7 action required), 21 lint + 82 gosec findings cleared (real fixes plus justified `// #nosec` on by-design sites), `x/text` bumped past GO-2026-5970, and GO-2026-5932 (x/crypto openpgp, fix N/A, init-only via opt-in OOB) carried as a documented, ID-scoped waiver that still fails on any other advisory.
- **Upgrade pass**: Go 1.26 toolchain pinned to go1.26.8 (`toolchain` directive — closes ~25 stdlib advisories found by govulncheck against the CI-installed 1.26.0), direct deps (`cobra` 1.10.2, `x/term` 0.46.0, `sqlite` 1.58.0) plus `x/net`/`miekg/dns` security floats, Actions refreshed to v7 majors (checkout/setup-go/upload-artifact; lint action v7 + linter v2.13.2), and Dependabot (gomod + actions, weekly) so pins stop rotting.

- **Wrapper correctness pass (verified live against owned targets)**: fixed wrong-flag specs (`shuffledns` gains `-mode bruteforce`, `dnsgen` takes a targets file, `hakrawler` runs `-d 2 -subs` over stdin, `cariddi` takes a positional target), wired four dormant binaries (`unfurl -u keys` with a new key-set parser, `csprecon -u -silent`, `metabigor cert`, `socialhunter` via a new `{urlsfile}` corpus template), and made `xsstrike` non-interactive (`--skip --skip-dom`). Parsers: takeover lines exclude help text (`potentially vulnerable`, `--hide_fails`), secret scans accept whole-document JSON arrays, empty tool output reports as quiet notes instead of errors, and stdin corpora lead with parameterized URLs.
- **Install recipes tell the truth**: corrected `gitleaks` (org move) and `gitjacker` (subpath) go paths, `ghauri`/`oralyzer` install from git (not on PyPI), script-only tools (`sstimap`, `graphqlmap`, `paramspider`, `corsy`, `linkfinder`, `openredirex`, dead `tplmap`/`nomore403`) and `jsluice` (unbuildable) point at manual clones, `semgrep`/`searchsploit` install for real (GitLab exploitdb home), and docker pulls verify via image inspect. Binary resolution now also checks the pipx user bin.
- **Interactive menu wizard**: after the level pick, the menu asks run mode (Standard/Ghost/Adversarial with explicit YES) and tool scope (all vs `--only` subset); `safe`/`advanced`/`ultra` shortcuts accept `--only/--enable/--disable`.
- **Report credibility fixes**: local-code findings (SAST/dependency/secret scans of operator-supplied code or APKs) are partitioned into an unscored appendix — they never enter target counts, diffs, history, or the risk score. Differential-only injection claims need corroboration (second probe or backend marker) or drop to Low/Medium-needs-review; single-technique SQLi boolean differentials no longer carry High. Same-site redirects (subdomains, `/_next/image` same-origin echoes) are not open redirects. External-tool run receipts are verbose-only instead of default Info findings.
- **Speed, intelligence, sequencing**: default `--parallel` is now 8 (audited snapshot-safe natives plus the wrapper pool overlap — wrappers only read finalized discovery data, smuggler stays sequential for timing). Runtime gates skip quietly with reasons instead of burning time: stack gates (`wpscan`/`droopescan`/`joomscan`/`cmseek` run only when the fingerprinted stack matches, fail-open on unknown), amass skips on rich native subdomain yield (timeout also cut 600s→180s), and parameter-less reflection tools skip when no parameterized URLs exist. Identical active differentials across sibling URLs merge into one finding. Classified parameter names test first. The pipeline now runs explicit phases (Foundation → Discovery → Targeted → Active) with headers, per-phase wall-time ledger in terminal and HTML, and deterministic in-order merging. External crawlers (gospider/hakrawler/cariddi/subjs) stay sequential: parallel unpaced crawls get WAF-throttled into empty results.

- **Wave 1 native engines (50, all built-in)**: passive recon pack (ArchiveURLs, CertSAN, EmailHarv, DNSAudit, EmailAuth, CSPRecon, SocialHijack, CommentMiner, BrokenLink, OriginIP — safe-profile eligible), exposure pack (ExposedGit detection-only, ExposedConfig, Actuator with headers-only heapdump, DebugPages, OAuthAnalyzer, SAMLMetadata, CSWSH with zero frames sent, PostMessage, JSSecrets with masked values, HiddenParams with dual baseline), technique pack (VerbTamper, CacheDeception, ForbiddenBypass, TakeoverPlus with 30 dnsReaper-family sigs, APIVersion, APIConsole, VHost), and vuln-expansion pack (GraphQLFP/Schema, JWTPlus decode-only, SSTIExpand 10 engines benign-math, NoSQLExpand differentials, H2Smuggle adversarial-gated safe-subset, RedirectPack no-follow, LFIPack marker+differential, CVEPack version-sigs + inert probe, CORSPlus credentialed matrix, AXFRPlus, BackupPlus 30 vendored suffixes, FaviconPlus, DebugMethods empty-body probes, EncodePoly canaries, DiffOracle repeat-stable, GFClassify offline). Every probe: explicit request budget, baseline-subtract, echo-guard, warn-and-skip.
- **Wave 2 wrappers (84 free binaries)**: one data-driven runner (`internal/integrations/generic.go`) with real read-only CLI vectors, timeout + 2MB caps, structured parsing (ffuf/gobuster/nikto/nmap/semgrep/trivy/osv/grype/wpscan/searchsploit), URL/host mining, and repro-carrying summary findings. Absent binaries warn-and-skip; adversarial tools (sqlmap, commix, git-dumper, …) need `--adversarial --confirm-authorized`; fuzzers need `ANPU_WORDLIST`, code tools `ANPU_CODE_DIR`. Toggles via `--only/--enable/--disable` and the `external:` YAML map; install recipes via `anpu tools install`.
- **Wave 3 feeds (keyless only)**: CISA KEV cache + scoring flag (`anpu feeds update`), FIRST EPSS opt-in (`ANPU_EPSS=1`), GitHub Advisory notes, URLhaus + ThreatFox reputation (`anpu feeds check`, surfaced as warnings), OSV Rust/RubyGems via Cargo.lock/Gemfile.lock harvest, vendored `wordlists/` subsets with opt-in refresh. PhishTank excluded (API needs a key).
- **Wave 4 workflow**: `anpu query` (finding filter), `anpu import` (Burp XML / ZAP JSON / HAR, import-only), notify fan-out (`watch --webhook/--discord/--telegram`), CSV/Markdown export (`show --format csv|md`), `--scope-file` allowlist hard-stop, per-tool budget ledger wired into the pipeline, and `anpu drift` baseline comparison with parser pins.
- **Operator-controlled safety (`--unsafe`)**: one master switch implying `--adversarial --confirm-authorized`, lifting safe-purity embedded filters, and running wrappers at fuller strength (sqlmap level/risk 2, nikto full-minus-DoS). Defaults, the authorization warning, and the hard exclusions (no brute-force, no exploit frameworks, no DoS, no destruction) are unchanged; single installs and `--auto-install` plans confirm before downloading (use `--yes` in scripts).
- **Next-wave natives (8, all built-in)**: Soap (WSDL/op harvest, no envelope POSTs), Nsecwalk (NSEC chain walk + NSEC3 detect, honors `ANPU_DNS_RESOLVER`), Wafdetect (canary block matrix + vendor ID), Oauthpack (redirect_uri confusion, Location never followed), Ppollute (merge-sink static analysis), Swscope (worker scope + foreign imports), Timeoracle (median-gated sleep differential), Jwtconfirm (alg:none acceptance differential, adversarial-gated). Every probe: explicit request budget, baseline + random control, echo-guard, warn-and-skip.
- **Schema-driven active rules + SBOM**: OpenAPI query examples are seeded into endpoint URLs (fixes bare-template gating for every schema rule) and vectors carry Schema/Required; three new Active rules (type-confusion, required-omission, content-confusion with garbage-type control). Deps emits CycloneDX 1.4 SBOMs into the output dir (also fixed: Deps now runs after Endpoints, so manifest harvest actually sees crawler URLs).
- **Codesecrets native**: offline tfstate/env/docker/k8s secret + misconfig review over `ANPU_CODE_DIR` (safe-eligible, 300 files, values masked); also wired as embedded fallback for the code-scope wrappers.
- **Parallel pipeline + resume**: `--parallel N` runs grep-audited snapshot-safe stages concurrently with in-order merging (proven identical output); `--checkpoint`/`--resume` persist per-stage results for interrupted scans.
- **Workflow depth**: `--risk-accept` YAML suppression with reason/expiry (expired entries re-arm), `--zap-ajax` for Docker ZAP runs, `drift --from-history` against stored scans.
- **smoke.ps1**: hermetic live verification (build → fixtures → 28 checks, exit code = failures) with zero test files — running code checking running code.

- **Release targets**: GoReleaser now produces native archives for Linux amd64/arm64, Windows amd64/arm64, and macOS amd64/arm64.
- **Release smoke test**: the release workflow downloads the published Linux amd64 archive and verifies `anpu --version` and `anpu --help` before the workflow completes.
- **CI self-test**: security scanning now runs only against a pinned local OWASP Juice Shop fixture and uploads generated reports as workflow artifacts instead of GitHub Code Scanning.
- **SARIF validation**: CI verifies SARIF 2.1.0 structure, ANPU tool identity, and non-empty results before archiving reports.
- **CI reliability**: workflow uses the Go version declared in `go.mod`, adds concurrency/timeout controls, and removes the floating Nuclei installation from the security workflow.
- **Build/supply-chain**: pure-Go SQLite via `modernc.org/sqlite`, reproducible Docker build settings, and automated vulnerability checking remain enabled.
- **Scanner engines**: added subdomain enumeration, port scanning, directory discovery, secret detection, CORS checks, and HTTP-method auditing with profile gating and accuracy safeguards.

## 0.3.0

- **Repo cleanup**: removed the entire vendored `third_party/` tree (~106 files of upstream cobra/pflag/yaml.v3/mousetrap sources and their replace directives). Dependencies now resolve normally from the Go module proxy, pinned by `go.sum`.
- **New engines (all built-in, zero external dependencies)**: subdomain enumeration via Certificate Transparency logs with optional DNS brute-forcing, a TCP connect port scanner for common service ports, sensitive-path content discovery with soft-404 baseline detection, JS/asset secrets hunting (AWS/GCP/GitHub/Slack keys, JWTs, private-key blocks), CORS origin-reflection auditing, and HTTP-method/XST verification.
- **Profile ladder**: `safe` is fully passive; `standard` adds the active-but-polite engines; `deep` enables DNS brute-force and the port scan. Every engine can be toggled individually via the `modules:` config section.
- **False-positive hardening for content discovery** (validated against live sites): dual soft-404 baselines with Jaccard similarity catch catch-all routers (SPAs, parked hosting); app-shell detection filters HTTP 200 copies of the site's own root page; WAF rejections (406) and intentional removals (410) are no longer reported as exposures.
- **Port-scan sanity probe**: when sanity-check ports 1/9 accept connections (transparent proxy answering for all ports), results are suppressed with a warning instead of reported.
- Added `anpu tools` doctor command listing built-in engines, optional integrations, and profile contents.
- Port-scan results now carry a CDN caveat when Cloudflare/CloudFront/Vercel/Fastly are fingerprinted, since scanned addresses may be the CDN edge rather than the origin.
- Sensitive-path findings distinguish readable exposure from 401/403 "present but protected" responses.
- New shared `DoWithHeaders` client method keeps custom probes (CORS/methods) behind the same SSRF guards as core requests.
- Added live-site integration test (`ANPU_LIVE_TESTS=1 go test -run TestLiveScanExample ./cmd/anpu`) that runs the full pipeline against example.com and validates the JSON report end-to-end.
- **Breaking Change**: Added a connectivity pre-check. Scans now immediately abort with a non-zero exit code if the target is completely unreachable (e.g., DNS failure, connection refused). Use `--skip-pre-check` to bypass this behavior.
- Switched the SQLite driver from `mattn/go-sqlite3` (cgo) to `modernc.org/sqlite` (pure Go). ANPU no longer requires a C compiler at build time and produces valid executables on all platforms out of the box; the old vendored `third_party/go-sqlite3` copy was removed.
- SARIF reports now serialize empty `rules`/`results` as `[]` instead of `null`, complying with the SARIF 2.1.0 schema.
- Fixed a bug where error messages were printed twice in the terminal.
- Added path to output filenames (`target-path-date.html`) to prevent collisions on same-host scans.
- Support config fallback for target URL when not passed via command line.
- Fixed `show --export` silencing output on successful writes.
- Upgraded `--verbose` output to show the true number of deduplicated findings each stage contributed.
- Clarified scoring documentation to explicitly mention Category Weight over Exposure.

## 0.2.1

- Clarified the status of OWASP ZAP integration (currently planned / interface defined) in documentation.
- Added comprehensive CI/CD documentation and example GitHub Actions workflow (`docs/ci-cd.md`).
- Added documentation for the transparent risk scoring algorithm (`docs/scoring.md`).
- Improved error messaging for local-network scanning attempts.
- Updated security policy with a vulnerability response timeline.

## 0.2.0

- Added `anpu diff <older> <newer>` for historical scan comparison.
- Added attack-surface diffing for endpoints and technologies, including technology version changes.
- Added finding added/removed/changed detection with risk-score deltas.
- Added `--fail-on none|low|medium|high|critical` for CI security gates.
- Reports and scan history are still written before a failed security gate returns non-zero.
- Added a compact risk grade to HTML reports.
- Centralized the release version as `pkg/version`.
- Updated README documentation and examples.
